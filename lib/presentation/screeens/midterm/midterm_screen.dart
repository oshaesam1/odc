import 'package:flutter/material.dart';

class MidtermScreen extends StatelessWidget {
  const MidtermScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Midterm Screen'),
      ),
    );
  }
}
